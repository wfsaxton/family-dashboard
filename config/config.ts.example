import { type CalendarConfig } from "@/modules/calendar/types";
import { type ClockConfig } from "@/modules/clock/types";
import { type TodolistConfig } from "@/modules/todolist/types";

export const calendarConfigs: CalendarConfig[] = [
  {
    id: "dadsemail@gmail.com",
    name: "Dad",
    color: "bg-green-500",
    isParent: true,
    isInitiallyDisplayed: true,
  },
  {
    id: "kidsemail@gmail.com",
    name: "Jane",
    color: "bg-purple-500",
    isParent: false,
    isInitiallyDisplayed: true,
  },
];

export const todolistConfigs: TodolistConfig[] = [
  {
    assigneeId: "12345678",
    assigneeName: "Jane",
    color: "bg-purple-500",
  },
];

export const clockConfig: ClockConfig = {
  refreshInterval: 60000,
}